<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All user data</title>
    <style>
        table,
        th,
        td{
            border: 1px solid blueviolet;
        }
    </style>
</head>
<body>
    <!-- Add button -->
    <form action="/user/new" method="get">
        <button>ADD new User</button>
    </form>


    <table>
        <tr>
            <th>id</th>
            <th>Email</th>
            <th>username</th>
        </tr>

        <% for(users of user) { %>
            <tr>
                <td><%= users.id %></td>
                <td><%= users.email %></td>
                <td><%= users.username %></td>
                
                <!-- edit button -->
                <td>
                    <form action="/user/<%=users.id%>/edit" method="get">
                    <button>Edit username</button>
                </form>

                </td>


                <td>
                    <form action="/user/<%=users.id%>/delete" method="get">
                    <button>Delete</button>
                </form>
                
            </tr>
            <% } %> 
    </table>
</body>
</html>